const config = {
    db: {
        user: 'student',
        password: 'student',
        connectString: 'localhost/XE',
        poolMin: 10,
        poolMax: 10,
        poolIncrement: 0,
    },
    express: {
        port: 3210
    }
}

module.exports = config;